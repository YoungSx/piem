import type { BuiltinSkillAsset } from "./builtinSkillPackage";

/** esbuild replaces this module with this build's digest, size and names only. */
export const BUILTIN_SKILL_ASSET: BuiltinSkillAsset | undefined = undefined;
export const BUILTIN_SKILLS_DEVELOPMENT: boolean = false;
