/** These two audited util imports already exist as browser primitives. */
const BrowserTextEncoder = TextEncoder;
const BrowserTextDecoder = TextDecoder;
export { BrowserTextEncoder as TextEncoder, BrowserTextDecoder as TextDecoder };
