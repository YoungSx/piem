# 默认诊断分享与关闭开关

用户指定 `https://otlppiem.shangxin.me` 为项目报告网关，鉴权由网关处理；默认开启，设置暂不开放 Collector 编辑。本次继续使用固定 Git 提交的原版 `b1tank/pi-otel`，没有移植或修改上游代码。

- 原生设置为 **分享错误报告与性能数据**。新装和缺省配置开启，显式 `false` 持久化；旧 `otelEndpoint` 不再生效。
- 关闭在等待保存前同步撤销所有会话的遥测观察者、后台资源与未发队列，不触发最后 flush，也不中断聊天。关闭决定在当前服务生命周期内保留；重新开启后重载恢复。已发出的 Obsidian 原生请求不能撤回。
- 清除观察者时保持 Runner 的扩展数组位置，不跳过其他扩展；仅释放它自己的事件监听，不回滚其他扩展的 flag。
- 设置、双语手册、README 和项目约束同步披露默认接收方、发送内容及正文默认关闭后仍可能包含内容的错误文字。

## 验证

[完整摘要](2026-09-12-default-diagnostics.json)。最终基线为 `5a4f26d`，产品提交 `51a0c61d7bfc2dea6729256e40d8296453599a6d`。

- `npm run verify` 全通过：**3813 tests / 258 files / 13356 assertions**，build、各静态门禁及 lint 通过。六个涉及行为的测试文件逐个独立运行通过。
- 专项测试覆盖默认三信号、固定地址、不传模型密钥、双会话即时关闭、初始化期间关闭不复活、Stop/新会话不恢复、关闭持久化，以及收集端拒绝连接或返回 503 时聊天继续。
- 正式 `main.js` **2,205,041 B**，SHA-256 `c8a877d95dac0896247b9313adab1a30c2320af3e39ac3d6e09077a0acb53796`。真实 Obsidian 桌面 **37 项**、官方手机模拟 **31 项**通过；原生开关显示默认开启，无 Collector 文本输入。
- 烟测保持产物原始字节与默认工厂，在 Obsidian 宿主 `requestUrl` 边界把固定域请求转发到一次性本地 HTTP 接收器。实际请求仍使用固定 HTTPS 地址，三信号均产生；关闭没有新增上报，false 重载仍关闭，计时器/监听/待处理请求最终全部为零。这是受控传输验证，不代表线上网关已收包。
- 线上域名 DNS 可解析，但本环境使用 Python、curl（TLS 1.2/1.3）、Node 与 Obsidian 浏览器访问时，均在 HTTPS 握手阶段失败，没有 HTTP 响应。只提交空 JSON 连通探针，未向线上发送真实笔记或凭据；未修改网关、证书或退回 HTTP。
- 一次性模型服务和接收器关闭；Obsidian/Xvfb 及派生进程全部退出；临时 vault/profile、原始合成收包已清理。
